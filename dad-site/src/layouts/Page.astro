---
export interface Props {
  title: string;
  bg: boolean;
}

const { title, bg } = Astro.props;

const year = new Date().getFullYear();
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body>
    <header>
      <h1 class="title"><a href="/">Product Plus</a> {title}</h1>
    </header>
    <!-- {
      bg && ( -->
    <div class="bg">
      <img class="logo" src="/logos/jbl.png" />
      <img class="logo" src="/logos/akg.png" />
      <img class="logo" src="/logos/jbl_pro.png" />
      <img class="logo" src="/logos/kin.png" />
      <img class="logo" src="/logos/marshal.png" />
      <img class="logo" src="/logos/arcam.svg" />
      <img class="logo" src="/logos/hk.svg" />
      <img class="logo" src="/logos/mark.svg" />
      <img class="logo" src="/logos/dbx.png" />
      <img class="logo" src="/logos/jaymar.svg" />
      <img class="logo" src="/logos/austere.png" />
      <img class="logo" src="/logos/crown.png" />
      <img class="logo" src="/logos/dual.png" />
      <img class="logo" src="/logos/fiio.png" />
      <img class="logo" src="/logos/flexson.png" />
      <img class="logo" src="/logos/human.png" />
      <img class="logo" src="/logos/lab.png" />
      <img class="logo" src="/logos/mission.png" />
      <img class="logo" src="/logos/one_for_all.svg" />
      <img class="logo" src="/logos/revel.svg" />
      <img class="logo" src="/logos/soundcraft.png" />
      <img class="logo" src="/logos/synth.svg" />
      <img class="logo" src="/logos/thorens.png" />
      <img class="logo" src="/logos/velodyne.png" />
    </div>
    <!-- )
    } -->
    <main>
      <slot />
      <footer>&copy; {year}, Brian Surrett. All rights reserved.</footer>
    </main>
    <style>
      *,
      *::before,
      *::after {
        /* 1. Use a more-intuitive box-sizing model. */
        box-sizing: border-box;
      }

      * {
        /* 2. Remove default margin */
        margin: 0;
      }

      html,
      body,
      main {
        /*3. Allow percentage-based heights in the application */
        height: 100vh;
        height: 100svh;
      }

      body {
        /*
          Typographic tweaks!
          4. Add accessible line-height
          5. Improve text rendering
        */
        line-height: 1.5;
        -webkit-font-smoothing: antialiased;
      }

      img,
      picture,
      video,
      canvas,
      svg {
        /* 6. Improve media defaults */
        display: block;
        max-width: 100%;
      }

      /*
  7. Remove built-in form typography styles
*/
      input,
      button,
      textarea,
      select {
        font: inherit;
      }
      /*
  8. Avoid text overflows
*/
      p,
      h1,
      h2,
      h3,
      h4,
      h5,
      h6 {
        overflow-wrap: break-word;
      }

      :root {
        --color-bg: white;
        --color-bg-alt: rgb(10, 75, 225);
        --color-border: black;
        --color-border-alt: black;
        --color-text: black;
        --color-text-alt: #bbb;
        --color-accent: rgb(10, 75, 225);
        --color-accent-alt: rgb(150, 10, 10);
        --font-size-base: clamp(1rem, 0.34vw + 0.91rem, 1.19rem);
        --font-size-lg: clamp(1.2rem, 0.7vw + 1.2rem, 1.5rem);
        --font-size-xl: clamp(1rem, 3vw + 1.5rem, 3.25rem);
      }

      body {
        min-width: 300;
        font-family: system-ui, sans-serif;
        font-size: var(--font-size-base);
        color: var(--color-text);
        background-color: var(--color-bg);
        overflow-y: hidden;
      }

      .bg {
        padding: 1rem;
        z-index: -1;
        position: absolute;
        top: 3rem;
        left: 0;
        bottom: 0;
        right: 0.5rem;
        display: flex;
        flex-wrap: wrap;
        gap: 0.75rem;
        justify-content: space-between;
        background-color: white;
        filter: blur(1px) brightness(var(--bg-br));
      }

      .logo {
        -moz-user-select: none;
        -webkit-user-select: none;
        max-width: 20vw;
        max-height: 12vh;
        user-select: none;
        object-fit: contain;
        color: black;
      }

      main {
        display: flex;
        flex-direction: column;
        overflow-y: auto;
        margin-top: 2.5rem;
        gap: 5rem;
        padding: 0.5rem;
        align-items: center;
        background-color: rgba(255, 255, 255, 0.25);
      }

      header {
        position: fixed;
        top: 0;
        height: 2.5rem;
        width: 100vw;
        color: #bbb;
        background-color: var(--color-bg-alt);
        z-index: 2;
        padding-left: 1rem;
      }

      .title {
        margin-top: 0.1rem;
        color: var(--color-text);
        font-size: x-large;
        color: #222;
      }

      .title a:hover {
        color: var(--color-accent-alt);
      }

      .title a {
        text-decoration: none;
        color: var(--color-text);
      }

      /* .wrap {
        position: absolute;
        left: 0;
        top: 3rem;
        right: 0;
        bottom: 0;
        display: flex;
        flex-wrap: wrap;
        gap: 1rem;
        justify-content: space-between;
        padding: 2rem;
        margin: 0;
        background-color: white;
        z-index: -1;
        filter: blur(1px) brightness(var(--bg-br));
      } */

      /* .logo {
        -moz-user-select: none;
        -webkit-user-select: none;
        user-select: none;
        min-width: 5rem;
        min-height: 5rem;
        max-width: 20rem;
        max-height: 10rem;
        object-fit: contain;
        color: black;
      } */

      footer {
        width: 100%;
        text-align: center;
        padding: 2rem;
        padding-bottom: 80vh;
        padding-bottom: 80svh;
        margin-top: 12rem;
      }

      @media (min-width: 768px) {
        main {
          display: grid;
          grid-template-rows: 1fr 1fr 1fr 1fr 0fr;
          grid-template-columns: 1fr 1fr;
          overflow-y: auto;
          margin-top: 2.5rem;
          gap: 2rem;
          padding: 5rem 16vw;
          align-items: center;
        }

        footer {
          grid-column: span 2;
        }
      }

      @media (min-width: 1200px) {
        main {
          grid-template-rows: 1fr 1fr 1fr 0fr;
          grid-template-columns: 1fr 1fr 1fr;
        }

        footer {
          grid-column: span 3;
        }
      }
    </style>

    <style is:global>
      h1 {
        font-size: var(--font-size-xl);
      }

      h2 {
        font-size: var(--font-size-lg);
      }

      code {
        font-family: Menlo, Monaco, Lucida Console, Liberation Mono, DejaVu Sans Mono, Bitstream Vera Sans Mono, Courier New, monospace;
      }
    </style>
  </body>
</html>
